# ============================================
# Business Affairs MCP Server Configuration
# ============================================
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Feature Flags
ENABLE_EMAIL=false
ENABLE_GDRIVE=false
ENABLE_WEBHOOKS=false

# Gmail API Integration
EMAIL_PROVIDER=gmail
GMAIL_CLIENT_ID=your_gmail_client_id_here
GMAIL_CLIENT_SECRET=your_gmail_client_secret_here
GMAIL_REDIRECT_URI=http://localhost:3000/oauth/callback
GMAIL_REFRESH_TOKEN=your_gmail_refresh_token_here

# Google Drive API Integration
GDRIVE_CLIENT_ID=your_gdrive_client_id_here
GDRIVE_CLIENT_SECRET=your_gdrive_client_secret_here
GDRIVE_REDIRECT_URI=http://localhost:3000/oauth/gdrive/callback
GDRIVE_REFRESH_TOKEN=your_gdrive_refresh_token_here

# Microsoft Outlook Integration (Alternative to Gmail)
OUTLOOK_CLIENT_ID=your_outlook_client_id_here
OUTLOOK_CLIENT_SECRET=your_outlook_client_secret_here
OUTLOOK_TENANT_ID=your_outlook_tenant_id_here
OUTLOOK_REDIRECT_URI=http://localhost:3000/oauth/outlook/callback

# Security
ENCRYPT_DATA=false
WEBHOOK_SECRET=your_webhook_secret_here

# Webhooks & Notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
TEAMS_WEBHOOK_URL=https://your-org.webhook.office.com/YOUR/WEBHOOK/URL

# AI/LLM Configuration (Future Use)
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=your_anthropic_api_key_here
AI_MODEL=claude-3-sonnet-20240229

# Logging
LOG_FILE=logs/app.log

# Timezone
TZ=UTC

# ============================================
# Integration Setup Instructions
# ============================================

# Gmail API Setup:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable Gmail API
# 4. Create OAuth 2.0 credentials
# 5. Add http://localhost:3000/oauth/callback to authorized redirect URIs
# 6. Download credentials and extract client_id and client_secret
# 7. Run OAuth flow to obtain refresh_token

# Google Drive API Setup:
# 1. In same Google Cloud Console project
# 2. Enable Google Drive API
# 3. Use same OAuth credentials or create new ones
# 4. Add Drive API scopes to consent screen
# 5. Obtain refresh_token through OAuth flow

# Microsoft Outlook Setup:
# 1. Go to https://portal.azure.com
# 2. Register new application
# 3. Add Microsoft Graph API permissions:
#    - Mail.Read
#    - Mail.Send
#    - User.Read
# 4. Create client secret
# 5. Configure redirect URI
# 6. Note down tenant_id, client_id, and client_secret

# Slack Webhook Setup:
# 1. Go to https://api.slack.com/apps
# 2. Create new app or select existing
# 3. Enable Incoming Webhooks
# 4. Add New Webhook to Workspace
# 5. Copy webhook URL

# Microsoft Teams Webhook Setup:
# 1. In Teams, go to target channel
# 2. Click ⋯ > Connectors
# 3. Configure Incoming Webhook
# 4. Provide name and upload image
# 5. Copy webhook URL
